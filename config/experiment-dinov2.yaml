# DINOv2 experiment configuration
# Properly-tuned DINOv2-Base with LLRD fine-tuning

project:
  name: "dinov2-multiscale"

model:
  backbone: "dinov2_base"

data:
  batch_size: 12  # Smaller batch for DINOv2 (768-dim vs 1024-dim per scale)
  scales: ["1x", "3x", "5x"]
